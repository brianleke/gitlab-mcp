# GitLab API Configuration
# Required: Your GitLab personal access token
# Get one at: https://gitlab.com/-/user_settings/personal_access_tokens
GITLAB_PRIVATE_TOKEN=your-gitlab-token-here

# MCP Server Security
# Required: Bearer token to authenticate access to the MCP server
# Generate a secure random token (e.g., using: openssl rand -hex 32)
MCP_BEARER_TOKEN=your-secure-bearer-token-here

# Optional: Set this if using a self-hosted GitLab instance
# Defaults to https://gitlab.com/api/v4
GITLAB_API_URL=https://gitlab.com/api/v4

