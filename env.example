# GitLab Configuration
# Get your personal access token from: https://gitlab.com/-/user_settings/personal_access_tokens
# Required scopes: api, read_api

GITLAB_URL=https://gitlab.com
GITLAB_TOKEN=your_personal_access_token_here

# API Bearer Token Authentication
# Set one or more bearer tokens (comma-separated) that clients must use to authenticate
# If not set, the GITLAB_TOKEN will be used as the bearer token (for backward compatibility)
# Example: API_BEARER_TOKENS=token1,token2,token3
API_BEARER_TOKENS=your_api_bearer_token_here
