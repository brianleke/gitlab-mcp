# GitLab Configuration
GITLAB_TOKEN=your_gitlab_personal_access_token_here
GITLAB_BASE_URL=https://gitlab.com/api/v4

# For self-hosted GitLab instances, use:
# GITLAB_BASE_URL=https://your-gitlab-instance.com/api/v4

# Server Bearer Token (optional but recommended for security)
# If set, clients must provide this token in Authorization header: "Bearer <token>"
# Leave empty to disable bearer token authentication
SERVER_BEARER_TOKEN=your_server_bearer_token_here

